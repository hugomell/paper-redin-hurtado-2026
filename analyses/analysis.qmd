---
title: "Analysis"
---

```{r}
#| output: false
library(targets)
library(dplyr)
```


Below is the first few rows of the data frame with composite scores:

```{r}
df <- tar_read(df_with_composites)
df |> select(Anxiety, Depression, ICSF) |> head()
```
